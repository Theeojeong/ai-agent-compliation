uv run agent.py 하기 전

아래 코드 실행

npx create-next-app@latest dashboard-app `
  --yes --typescript --eslint --tailwind --app --src-dir `
  --import-alias "@/*" --use-npm --no-react-compiler

# -----------------------

shadcn-dashboard 경로로 들어가서

npm run dev 실행